{
  "name": "src",
  "version": "1.0.0",
  "description": "MCP server for 3D scene generation for web and ads.",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "nodemon --watch src --ext ts --exec \"npm run build && node dist/server.js\"",
    "dev:http": "MCP_TRANSPORT=http nodemon --watch src --ext ts --exec \"npm run build && node dist/server.js\"",
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "fastmcp": "^3.34.0",
    "uuid": "^13.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "nodemon": "^3.1.10",
    "typescript": "^5.9.3"
  }
}
